<%= form_with(model: student) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :class %>
    <%= form.text_field :class %>
  </div>

  <div class="field">
    <%= form.label :roll_no %>
    <%= form.text_field :roll_no %>
  </div>

  <div>
    <%= form.fields_for :subjects do |subject|%>
      <%= render 'subject_fields', f: task%> 
    <%end%>
  </div>

  <div class="link">
      <%= link_to_add_association 'add task', form, :subjects %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
